name : first-workflow
on : workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name : first-step
        run: echo "first step"
      - name : second-step
        run: echo "second step"
        
